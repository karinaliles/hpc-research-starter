"""Evaluate trained models and compute interpretation outputs (e.g. SHAP).

Fill this in from templates/methodology.md Question 5 (how you know it worked).

AI prompt: "Read templates/methodology.md and implement evaluate.py: load the trained
model and results/metrics.json, compute the evaluation metrics, run SHAP, and
save shap values to results/."
"""


def main():
    # TODO: load model + results, compute metrics, run SHAP, save to results/
    raise NotImplementedError(
        "See templates/methodology.md, then ask your AI tool to implement this."
    )


if __name__ == "__main__":
    main()
